<%- contentFor('bodyPage')%>
<title>SWOON</title>

<body>
    <!-- TODO if user is not logged in, load this -->
    <% if(user == undefined) {%>
        <div style="width: 100%; height: 100%; overflow: hidden; position: absolute; top: 60px; border: none; z-index:1;">
            <a href="#" class="button-clubs"></a>

            <button onclick="location.href = '/searchPage'" class="button-find">Find Clubs</button>

            <img src="img/lajolla2.png" alt="Responsive image" alt="Responsive image" style="min-width: 400px; min-height: 100%; max-height: 150%; border: none; position: relative;"/>
        </div>
    <% } %>
    <!-- TODO else, load this -->
    <% if(user != undefined) {%>
        <div style="width: 70%; height: 100%; overflow: hidden; position: absolute; top: 60px; border: none; z-index:1;">
            <a href="#" class="button-clubs"></a>

            <button onclick="location.href = '/searchPage'" class="button-find">Find Clubs</button>

            <img src="img/lajolla2.png" alt="Responsive image" style="min-width: 400px; min-height: 100%; max-height: 150%; border: none; position: relative;"/>
        </div>

        <div style="width: 30%; height: 100%; overflow: hidden; position: absolute; right: 0; bottom: -60px; z-index:1;">
            <a href="#" class="button-clubs"></a>

            <button onclick="location.href = '/users/editUserProfile'" type="button"  class="button-small">Profile</button>

            <img src="img/geisel.jpg" style="min-width: 400px; max-width: 5000px; min-height: 100%; max-height: 100%; left: -300px; position: relative;"/>
        </div>
    <% } %>
</body>